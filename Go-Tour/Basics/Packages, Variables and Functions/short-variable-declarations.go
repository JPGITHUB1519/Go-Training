package main

import "fmt"

func main() {
	var i, j, int = 1, 2
	// inside a function a variable can be initialized with this short-hand, outside a function no
	k := 3
	c, python, java := true, false, "no!"
	fmt.Println(i, j, k, c, python, java)
}
